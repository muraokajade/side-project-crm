<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 環境全体のアクセス制限(Basic認証)
    |--------------------------------------------------------------------------
    |
    | ベータ公開中、URLを知っているだけの第三者が環境へ到達しないようにするための
    | 共有パスワード。App\Http\Middleware\EnsureCrmAccess が参照する。
    |
    | 利用者個人を識別しないため、アプリ内ログイン(認証・所有者分離)の代替にはならない。
    | password が空文字の場合、Basic認証は無効(素通り)になる。
    |
    | env()はconfigファイル内でのみ呼ぶ。ミドルウェアから直接env()を読むと、
    | config:cache 実行後にnullが返り、Basic認証が無言で無効化されてしまう。
    |
    */

    'user' => (string) env('APP_ACCESS_USER', 'admin'),

    'password' => (string) env('APP_ACCESS_PASSWORD', ''),

];
